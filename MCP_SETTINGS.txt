[mcp_servers]
last_hope = { command = "node", args = ["server.js"], cwd = "." }

# Qoder MCP setup (example JSON to paste in Qoder MCP settings)
{
  "mcpServers": {
    "last-hope": {
      "command": "node",
      "args": ["server.js"],
      "cwd": "."
    }
  }
}

# CLI usage
# - ./cli_setup.sh → локальная настройка + Terraform apply
# - gh api repos/:owner/:repo → direct API calls
# - spec apply spec.yml → применить GitHub конфиг
